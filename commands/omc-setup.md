---
description: "Setup and configure oh-my-claudecode (the ONLY command you need to learn)"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:omc-setup
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:omc-setup <your arguments here>
```
