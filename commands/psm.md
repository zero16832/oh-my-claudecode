---
description: "Project Session Manager - isolated dev environments with git worktrees and tmux"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:psm
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:psm <your arguments here>
```
