---
description: "Initialize a PRD (Product Requirements Document) for structured ralph-loop execution"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:ralph-init
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:ralph-init <your arguments here>
```
