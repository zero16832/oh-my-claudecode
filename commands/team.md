---
description: "N coordinated agents on shared task list using Claude Code native teams"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:team
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:team <your arguments here>
```
