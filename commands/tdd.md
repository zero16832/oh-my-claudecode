---
description: "Start Test-Driven Development workflow"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:tdd
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:tdd <your arguments here>
```
