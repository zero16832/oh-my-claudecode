---
description: "Sequential agent chaining with data passing between stages"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:pipeline
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:pipeline <your arguments here>
```
