---
description: "N coordinated agents on shared task list (compatibility facade over team)"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:swarm
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:swarm <your arguments here>
```
