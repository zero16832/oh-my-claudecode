---
description: "Token-efficient parallel execution mode using Haiku and Sonnet agents"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:ecomode
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:ecomode <your arguments here>
```
