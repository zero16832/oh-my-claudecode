---
description: "Cancel any active OMC mode (autopilot, ralph, ultrawork, ecomode, ultraqa, swarm, ultrapilot, pipeline, team)"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:cancel
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:cancel <your arguments here>
```
