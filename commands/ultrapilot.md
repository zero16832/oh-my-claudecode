---
description: "Parallel autopilot with file ownership partitioning (up to 5x faster)"
---

This command is deprecated. Invoke the skill instead:

```
/oh-my-claudecode:ultrapilot
```

Pass any arguments after the skill name. For example:

```
/oh-my-claudecode:ultrapilot <your arguments here>
```
